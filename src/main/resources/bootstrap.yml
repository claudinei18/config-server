server:
  port: 8888

spring:
  profiles:
    active: native

---
spring:
  profiles: git
  cloud:
    config:
      server:
        git:
          uri: git@antares:siccon/siccon-config.git

---
spring:
  profiles: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///xdr/siccon/config
