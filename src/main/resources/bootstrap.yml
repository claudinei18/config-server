server:
  port: 8888

spring:
  profiles:
    active: native

---
spring:
  profiles: git
  cloud:
    config:
      server:
        git:
          uri: git@github.com:claudinei18/config.git

---
spring:
  profiles: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///xdr/siccon/config
